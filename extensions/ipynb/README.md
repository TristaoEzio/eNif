# Jupyter for eNif

**Notice:** This extension is bundled with eNif. It can be disabled but not uninstalled.

## Features

This extension provides the following Jupyter-related features for eNif:

- Open, edit and save .ipynb files
